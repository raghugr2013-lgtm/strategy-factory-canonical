"""api.latent — namespace for dormant institutional infrastructure.

Routes under `/api/latent/*` are reserved for diagnostic / introspection
of latent capabilities. They MUST NOT take authoritative actions; their
purpose is to expose feature-flag state, evidence collection, and
calibration tables for operator review.
"""
