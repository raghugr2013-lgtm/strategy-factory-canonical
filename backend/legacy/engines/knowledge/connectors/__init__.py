"""Phase 2 Stage 3.α — Concrete `KnowledgeConnector` implementations.

Adding a new connector: create `connectors/<name>.py`, implement the
`KnowledgeConnector` Protocol, and register the instance in
`engines/knowledge/registry.py`. No changes to the Protocol, the
domain registry, or the pipeline are required.
"""
