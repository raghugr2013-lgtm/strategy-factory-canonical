import React from 'react';
import { TimeWindowChip } from './TimeWindowChip';

export default {
  title: 'Features/TimeWindowChip',
  component: TimeWindowChip,
  parameters: { docs: { description: { component: 'F2 · time-window control · workspace-scoped.' } } },
};

export const Default = { args: {} };
