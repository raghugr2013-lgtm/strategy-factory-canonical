import React from 'react';
import { SurfaceStub } from './SurfaceStub';

export const Approvals = () => (
  <SurfaceStub testId="approvals"
               headline="Approvals arrive with the full evidence bundle already attached."
               briefing="The Approval Center unifies Meta-Learning recommendations, Factory-Eval promotions, and governance advisories into one ranked queue with optimistic UI, provenance triples, and session-memory of resolved decisions."
               milestone="M4" />
);
