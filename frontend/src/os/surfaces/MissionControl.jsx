import React from 'react';
import { SurfaceStub } from './SurfaceStub';

export const MissionControl = () => (
  <SurfaceStub testId="mission-control"
               headline="A busy shift. The Factory needs three human decisions."
               briefing="Mission Control answers the six operator questions in a single glance — what is live, what's under way, what needs a human decision, what to watch, what to trust, and where attention should focus next."
               milestone="M4" />
);
