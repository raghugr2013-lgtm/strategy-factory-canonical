import React from 'react';
import { SurfaceStub } from './SurfaceStub';

export const Strategies = () => (
  <SurfaceStub testId="strategies"
               headline="Every strategy. One table. Click through to a full passport."
               briefing="The Strategy Explorer holds the flat, sortable, filterable inventory of every strategy the Factory has ever produced — with inline provenance chips and status facets that cascade back into Timeline and Approvals."
               milestone="M4" />
);
