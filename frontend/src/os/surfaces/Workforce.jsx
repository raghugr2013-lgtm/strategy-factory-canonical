import React from 'react';
import { SurfaceStub } from './SurfaceStub';

export const Workforce = () => (
  <SurfaceStub testId="workforce"
               headline="Coordinates every research plan across ingest, feature, signal, backtest."
               briefing="The AI Workforce surfaces every worker's role, current step, and health — with cross-links into the plans they're serving and the strategies they're producing."
               milestone="Sprint 2" />
);
