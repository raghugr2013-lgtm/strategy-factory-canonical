import React from 'react';
import { SurfaceStub } from './SurfaceStub';

export const Settings = () => (
  <SurfaceStub testId="settings"
               headline="Personalization mode, density, and shortcuts live in the header user menu."
               briefing="Sprint 1 keeps personalization inline in the shell. A dedicated settings surface arrives with role-scoped preferences in Sprint 2+."
               milestone="Sprint 2" />
);
