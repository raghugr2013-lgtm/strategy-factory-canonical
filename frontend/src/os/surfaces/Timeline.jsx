import React from 'react';
import { SurfaceStub } from './SurfaceStub';

export const Timeline = () => (
  <SurfaceStub testId="timeline"
               headline="Every action, every actor, one chronological stream."
               briefing="The Activity Timeline unifies research plans, ingest jobs, feature builds, backtests, governance holds, and approvals into a single ranked feed. Facet selections persist across surfaces — actor here, risk in Approvals, status in Strategies."
               milestone="M4" />
);
