# Modules — v1.1 owned frontend extension barrel
#
# This directory is OUTSIDE the frozen v01 `components/` and `command/` trees.
# Future modules register themselves via `src/modules/index.js`, which is
# imported optionally by the CommandShell registry at boot.
#
# Do not add anything here until an integration PR lands.
